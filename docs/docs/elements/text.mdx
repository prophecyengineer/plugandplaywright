# Text 

## `getText`

Description: Retrieves the visible text from a specific element on the page. It's essential for validating that the UI displays the expected text content to users.


Example: `pick up the text right here`

```jsx {5}
import { getText } from 'plugandplaywright'

export async function layoutCheck(page) {

    await getText(page, "pick up the text right here")

}
```

## `getTextPartialMatch`



However for times you'd prefer a partial match, we've got you covered.


```jsx {5}
import { getTextPartialMatch } from 'plugandplaywright'

export async function layoutCheck(page) {

    await getTextPartialMatch(page, "xt right here")

}

```


## `hoverText`

Test: Hover over `this cool text here`.


```jsx {5}
import { hoverText } from 'plugandplaywright'

export async function layoutCheck(page) {

    await hoverText(page, "this cool text here")

}
```


## `hoverTextPartialMatch`

Test: Hover over `this cool text here`.


```jsx {5}
import { hoverText } from 'plugandplaywright'

export async function layoutCheck(page) {

    await hoverTextPartialMatch(page, " cool text ")

}
```